server:
    port: ${SERVER_PORT:8084}
spring:
    application:
        name: ms-catalogo
    jpa:
        open-in-view: false
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.H2Dialect
    h2:
        console:
            enabled: true

